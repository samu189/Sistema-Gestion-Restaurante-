server:
  port: 8091

spring:
  application:
    name: ms-notificaciones
  datasource:
    url: jdbc:mysql://localhost:3306/db_notificaciones
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true